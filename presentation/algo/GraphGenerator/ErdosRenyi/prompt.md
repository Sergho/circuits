# Промпт

Реализуй в проекте `circuits.core` (директория `circuits.core/GraphGenerator/`)
класс `ErdosRenyiProbabilityGenerator`, реализующий интерфейс `IGraphGenerator`.

Генератор строит случайный граф по модели Эрдёша–Реньи G(n, p): для каждой пары
вершин `(i, j)` с `1 ≤ i < j ≤ verticesCount` независимо с вероятностью
`edgeProbability` добавляется ребро.

Параметры конструктора:

- `int verticesCount` — количество вершин, ≥ 0.
- `double edgeProbability` — вероятность ребра, в диапазоне `[0, 1]`.

Если параметры некорректны, выбрасывай `ArgumentException` с понятным сообщением
на русском языке.

Для случайных чисел используй `Random`, инициализированный в конструкторе через
`new()` без seed.

Используй соглашения по коду и доменные типы из `../context.md`.
